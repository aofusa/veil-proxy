# すべて除外（ホワイトリストの基盤）
**

# 必要なファイルだけ許可
!Cargo.toml
!Cargo.lock
# 同梱 config.toml（設定リファレンス。ビルドコンテキストに含めておく）
!config.toml
!src/
!docker/assets
# fuzz ワークスペースメンバ（[workspace] members = [".", "fuzz"]）の解決に必要。
# 本体はコンテナではビルドしないが、マニフェストとターゲットが無いと cargo が
# ワークスペース解決に失敗する。
!fuzz/Cargo.toml
!fuzz/fuzz_targets
