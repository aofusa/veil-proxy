# nghttp3-sys

veil 向け **nghttp3** の低レベル FFI。

- ソース: 公式 <https://github.com/ngtcp2/nghttp3>（タグは `build.rs` の `NGHTTP3_VERSION`）
- バインディング: `src/bindings.rs`（再生成: `cargo build -p nghttp3-sys --features overwrite`）
